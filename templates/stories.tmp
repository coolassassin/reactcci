import React from 'react';
import { #NAME# } from './#NAME#';

export default { title: '#STORY_PATH#' };

const defaultProps: React.ComponentProps<typeof #NAME#> = {

};

export const Overview = () => {
    return (
        <#NAME# {...defaultProps} />
    );
};
